# Changelog

## v2.0.0 — 2026-09-24

**新增图形界面（tkinter）**

- 拖模板表进窗口即可生成；拖拽组件不可用时自动降级为「选择文件」按钮
- 输出**固定写在模板表所在的文件夹**
- 界面内自带「使用教程」与「常见问题」两个页签，图示为矢量绘制
- 错误分三级：必须改（停下并一次列全）/ 要确认（弹窗）/ 只提示（照常生成）
- 每条错误都带「行号 + 单元格 + 怎么改」

**溢出检查升级**

- 点名是**哪一段**最长、超几行、**大概删几个字符**能收回一页
- 弹窗讲清后果：会被拆到两页，且因后面跟着硬分页符，后面唛头整体错位、页码跟着变

**架构**

- `validate` / `check_pl` / `check_overflow` 增加 `sink` 钩子：GUI 与 CLI 共用同一套校验，
  命令行行为保持不变
- 配置与母本支持「旁边那份 → 打包进 exe 那份」兜底
- 一个 exe 同时是 GUI 与命令行（无参数开窗口，带 `-i/-o` 走命令行）

**打包**

- 改用 `--onedir`：onefile 启动 97 秒 → onedir 1.1 秒
- windowed 模式三件套：`_fix_streams()` / `_report_sink()` / `_install_crash_handler()`

**测试**

- 三套：`--self-test`（13 用例）/ `run_tests.py` / `gui_tests.py`（14 项，含端到端）
- 端到端断言：输出落在模板同目录、页数正确、重复生成可覆盖

## v1.0.0 — 2026-09-24

首个发行版（命令行）。

- 从 PL 箱单对齐的模板表生成 Word 唛头，一件一张、块间自动分页
- 字段别名、向下继承、自动大写、件数与 PL 箱单核对
- 字符与格式鲁棒性：XML 转义、`\n` → `<w:br/>`、剔除非法控制字符、文本/浮点/全角件数
- 单页溢出估算（按 Times New Roman 字宽表 + 母本页面几何）
