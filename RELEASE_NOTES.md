# 唛头生成器 v2.0.0 · Shipping Mark Generator

把 Excel 装箱表变成**一页一张、自动分页**的 Word 唛头（Shipping Marks）。
Turn an Excel packing list into print-ready Word shipping marks.

---

## 下载与使用 / Download & Run

1. 下载下面的 **`shipping-mark-generator_v2.0.0.zip`**
2. **整个解压**到一个文件夹（别只把 exe 拖出来 —— 它旁边的 `_internal\` 是必需的）
3. 双击 **`唛头生成器.exe`**
4. 把 `唛头模板表.xlsx` 拖进窗口 → 点「开始生成」

> **不用装 Python，不用装 Office 宏。** Windows 10/11 直接跑。
> No Python required — just unzip and run.

窗口里有「**使用教程**」和「**常见问题**」两个页签，讲得比说明文件还细。

---

## 包里有什么

| 文件 | 说明 |
|---|---|
| `唛头生成器.exe` | 主程序（图形界面 + 命令行二合一） |
| `_internal\` | 运行必需的组件，**别删别挪** |
| `mark_config.json` | 配置：字段别名、大写范围、PL 核对、溢出检查 |
| `唛头母本.docx` | 版式母本 —— 字体/字号/页边距都取自它，改它就能改版式 |
| `唛头模板表.xlsx` | 示例输入表（带「使用说明」sheet） |
| `使用说明.txt` | 文字版说明 |
| `1-生成唛头.bat` … `5-拖模板到这里生成.bat` | 一键入口（第 5 个不用开界面，直接把模板表拖上去就出唛头） |
| `测试用例\` | 13 个用例 + 两个回归脚本 |

---

## 这一版有什么

### 图形界面（新增）

- 拖模板表进窗口即可生成；拖拽组件不可用时自动降级为按钮选择
- **输出固定写在模板表所在的文件夹**，不用选路径
- 界面内自带「使用教程」「常见问题」，图示是矢量画的（缩放不糊）

### 报错说人话

错误分三级，每条都带「**第几行 + 哪个单元格 + 改成什么**」：

| 级别 | 情况 | 行为 |
|---|---|---|
| 必须改 | 必填项空 / 件数不是 ≥1 的整数 / 公式没缓存值 | 停下，一次列全 |
| 要确认 | 件数与 PL 箱单对不上 / 唛头太长一页放不下 | 弹窗讲清后果，你决定 |
| 只提示 | 表头有多余列 / 票名行不连续 / 找不到 PL 文件 | 照常生成 |

### 内容太长会怎样（新）

程序按母本字号 + 页边距 + 字体宽度**估**每个唛头占多少行（默认约 24 行）。
估超了会**点名是哪一段最长、超几行、大概删几个字符能收回一页**：

```
票「TEST-OVER-1」的唛头 2-1（源第 2 行）预估 27.0 行 > 单页容量 24.0 行，会溢出到第二页
→ 最长的是「CNEE'S ADDRESS」这一段（多占 3.0 行）—— 把它缩短约 233 个字符就能收回一页。
```

**后果**：那个唛头会被拆到两页；因为它后面跟着硬分页符，后面的唛头会整体错位、页码也跟着变。
想让它「超了就干脆不让生成」：把 `mark_config.json` 里 `overflow_check.on_overflow` 改成 `"error"`。

### 命令行（同一个 exe）

```bat
唛头生成器.exe -i 模板表.xlsx -o 输出.docx    直接生成
唛头生成器.exe --dry-run                      只检查不写文件
唛头生成器.exe --no-pl-check                  跳过件数与三单/四单 PL 的核对
唛头生成器.exe --self-test                    跑「测试用例」里的全部用例
唛头生成器.exe -V                             看版本
```

---

## 质量

三套测试全绿：

| 测试 | 结果 |
|---|---|
| `唛头生成器.exe --self-test` | 13/13 通过 |
| `测试用例\run_tests.py` | 13/13 通过 |
| `测试用例\gui_tests.py` | 14/14 通过（含端到端：落在模板同目录、页数正确、可覆盖） |

覆盖：一票多物 / 多票一物 / 票名不连续 / 字符兼容（`& < > " — 全角 emoji 零宽空格 控制字符`）/
特殊格式 / 单页溢出 / 多余列 / 最小单张 / 必填缺失 / 件数非法 / 缺列 / 公式无缓存。

---

## 校验和

```
SHA256  c428ebd57571143dc20400fdbb2f5b2cf874a294c84f280d3eadc640aac5503c
```

（Windows 里核对：`certutil -hashfile shipping-mark-generator_v2.0.0.zip SHA256`）

---

## 从源码跑

```bash
pip install openpyxl
python make_mark_gui.py     # 图形界面
python make_mark.py         # 命令行
```

MIT License · 源码见仓库根目录，完整改动见 [CHANGELOG.md](CHANGELOG.md)
